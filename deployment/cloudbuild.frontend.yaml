steps:
  - name: gcr.io/cloud-builders/docker
    dir: frontend
    args:
      - build
      - --build-arg
      - VITE_API_BASE_URL=$_VITE_API_BASE_URL
      - -t
      - $_IMAGE
      - .

images:
  - $_IMAGE
